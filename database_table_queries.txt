please set oracle database username and password:-

username: system
password: 71687

 CREATE TABLE  "BILL_SUBMIT" 
   (	"USERNAME" VARCHAR2(25) NOT NULL ENABLE, 
	"BILL_NO" NUMBER NOT NULL ENABLE, 
	"BILL_DETAILS" VARCHAR2(100) NOT NULL ENABLE, 
	"MANAGER_NAME" VARCHAR2(35) NOT NULL ENABLE, 
	"DEPT_TYPE" VARCHAR2(20) NOT NULL ENABLE, 
	"NAME" VARCHAR2(40) NOT NULL ENABLE, 
	 CONSTRAINT "BILL_NO_PK" PRIMARY KEY ("BILL_NO") ENABLE
   )
/

CREATE TABLE  "BILL_REPORT" 
   (	"USERNAME" VARCHAR2(25) NOT NULL ENABLE, 
	"BILL_NUM" NUMBER(*,0) NOT NULL ENABLE, 
	"BILL_DETAILS" VARCHAR2(100) NOT NULL ENABLE, 
	"DEPT_TYPE" VARCHAR2(15) NOT NULL ENABLE, 
	"STATUS" VARCHAR2(60) NOT NULL ENABLE, 
	 CONSTRAINT "BILL_NUM_PK" PRIMARY KEY ("BILL_NUM") ENABLE
   )
/

CREATE TABLE  "LOGIN" 
   (	"NAME" VARCHAR2(35) NOT NULL ENABLE, 
	"MOBILE_NO" VARCHAR2(15) NOT NULL ENABLE, 
	"EMAIL_ID" VARCHAR2(35) NOT NULL ENABLE, 
	"USERNAME" VARCHAR2(25) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(25) NOT NULL ENABLE, 
	"LOGIN_TYPE" VARCHAR2(20) NOT NULL ENABLE, 
	"SECURITY_ANS" VARCHAR2(35) NOT NULL ENABLE, 
	"DEPT_TYPE" VARCHAR2(20), 
	 CONSTRAINT "USERNAME_PK" PRIMARY KEY ("USERNAME") ENABLE
   )
/

		
CREATE TABLE  "FINANCE_BILL" 
   (	"USERNAME" VARCHAR2(25) NOT NULL ENABLE, 
	"BILL_NO" NUMBER(*,0) NOT NULL ENABLE, 
	"BILL_DETAILS" VARCHAR2(100) NOT NULL ENABLE, 
	"MANAGER_NAME" VARCHAR2(35) NOT NULL ENABLE, 
	"DEPT_TYPE" VARCHAR2(15) NOT NULL ENABLE, 
	"NAME" VARCHAR2(35) NOT NULL ENABLE, 
	 CONSTRAINT "BILL_NO_UQ" UNIQUE ("BILL_NO") ENABLE
   )
/

		